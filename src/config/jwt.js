// Placeholder — JWT signing/verification configuration.
// Implemented in the Authentication phase (Phase 1).
// Approved design: httpOnly cookie transport, 2-hour access token expiry,
// token_version claim for revocation. See docs/PROJECT_BLUEPRINT.md, Section 9.
